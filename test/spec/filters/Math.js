'use strict';

//describe('Filter: Math', function () {
//
//  // load the filter's module
//  beforeEach(module('crackooApp'));
//
//  // initialize a new instance of the filter before each test
//  var Math;
//  //beforeEach(inject(function($filter) {
////    Math = $filter('Math');
//  //}));
//
//  it('should return the input prefixed with "Math filter:"', function () {
//    //var text = 'angularjs';
//    //expect(Math(text)).toBe('Math filter: ' + text);
//  });
//
//});
